# Task 3 V2 合成记录

本目录登记 `task3-2026-07-19-v2.jsonl` 的八模型乘三角色合成矩阵。音频实际位于被 Git 忽略的 `cloneData/audio_v2/`；`synthesis.jsonl` 冻结每条音频的 SHA-256、采样率、通道数、时长、推理入口和配置哈希。

这些记录只对应本轮已生成的字节内容。重新合成后必须创建新的 `run_id` 并登记新哈希，不得覆盖音频后继续沿用这里的记录。
