# Seed-TTS 运行阻塞记录

状态：**等待人工确认，未启动官方 Seed-TTS 中文基准合成。**

本文记录 [Seed-TTS评测任务.md](Seed-TTS评测任务.md) 所定义的外部 Seed-TTS 中文基准为什么尚未执行，以及恢复执行前需要确认的事项。它已从 task8/task9 的长音频公共评测中拆分；没有以现有长音频替代该基准，也没有生成任何官方基准的 `utt.wav`。

## 已确认的基准范围

- 官方中文清单由 `zh/meta.lst`（2,020 条）与 `zh/hardcase.lst`（400 条）组成，共 2,420 条文本。
- 评测对象为 7 个模型，因此需要逐条生成共 16,940 个 `utt.wav`。
- 每个模型的合成结果必须保留在独立目录，再分别通过 `seed_tts_eval` 环境计算 Paraformer WER（词错误率），通过 `seed_tts_sim` 环境计算 WavLM-large-SV SIM（说话人相似度）。
- 当前仓库内的 `longAudioTestV9/audio_*.wav` 是长音频公共评测输入；任务规则明确禁止把它们用作 Seed-TTS 官方基准的替代数据。

## 当前阻塞项

### 1. 缺少两个模型的可运行官方实现

- **IndexTTS2**：目标环境中未安装 `indextts` 包，亦未配置可用的官方源码目录（如 `INDEXTTS_CODE_PATH`）。
- **LongCat-AudioDiT-1B**：目标环境中未安装 `audiodit` 包，也没有可用的官方 LongCat-AudioDiT 源码目录。

其余模型的部分运行时虽然可被发现，但尚未逐个完成官方基准格式的最小合成验证；不能据此宣称七模型均可批量执行。

### 2. 尚无官方基准合成产物

已检查官方中文集对应目录，当前没有任何模型的逐条 `utt.wav`，也没有已有的 WER 或 SIM 原始结果可供复用。因此不能仅运行打分脚本完成该项。

### 3. Seed-TTS 评测脚本尚未形成冻结的本地兼容补丁

本地存在 Seed-TTS 评测源码、测试集、Paraformer 与 WavLM-large-SV 所需资源，且下列隔离环境已完成基础导入检查：

- `seed_tts_eval`：用于 Paraformer WER。
- `seed_tts_sim`：用于官方说话人相似度入口。

但官方脚本仍需要针对本机环境完成并审阅兼容改造（包括本地 Paraformer 调用与非交互式文件拆分行为），再将改动、脚本哈希和运行命令冻结到评测记录中。当前没有可复现的补丁清单，不能直接开始正式批处理。

### 4. 执行规模需要明确授权

补齐缺失的模型源码/依赖可能需要下载和安装；随后还会占用 GPU 进行 16,940 条逐条合成，并产生大量音频文件。根据项目安全与评测复现约束，这些都不应在未确认的情况下自行执行。

## 已完成且不受阻塞影响的工作

长音频公共评测已按受限口径完成，产物位于 `longAudioTestV9/评测结果/`：

- `SenseVoice_CER&Whisper-large-v3-turbo_CER_V9评价报告.md`
- `音频交付与文本一致性_V9自动检查报告.md`

其中历史 Seed-TTS 占位报告仅说明拆分前的未执行状态；今后的 Seed-TTS 报告不再写入长音频目录。

正式长音频结果只包含音频交付观测、SenseVoice CER 与 Whisper-large-v3-turbo CER；没有生成被规则禁止的长音频 SIM、UTMOSv2、NISQA、自动总分或总排名。

## 恢复执行前，请人工确认

请在后续讨论中明确以下事项：

1. 是否授权下载/安装 IndexTTS2 与 LongCat-AudioDiT-1B 的官方运行时及其依赖；若已有离线源码或模型缓存，请提供位置或接入方式。
2. 是否授权使用 GPU 执行 16,940 条官方中文测试文本的逐条合成，并确认音频输出目录与可接受的磁盘占用。
3. 是否同意先完成 Seed-TTS 脚本的本地兼容补丁、最小样本冒烟测试和补丁哈希记录，再启动正式批量运行。

在获得这些确认前，保持 Seed-TTS 基准为未执行状态。
