# 公共评测任务的环境和配置前提

本文汇总执行 [公共评测任务.md](公共评测任务.md) 前所需的软件、隔离环境、本地资源、环境变量和冻结项，供 [`task8.md`](task8.md) 与 [`task9.md`](task9.md) 共用。它是配置说明，不会执行合成、加载评测权重或计算任何评分。

## 1. 评测边界与环境分工

| 工作内容 | 唯一环境 | 输入与本地权重 | 结果边界 |
| --- | --- | --- | --- |
| Seed-TTS 中文 WER（词错误率，逐字 token） | `seed_tts_eval` | 官方中文常规集 / 难例集、`funasr/paraformer-zh` | 只比较相同官方分集中的文本保真；不与长音频 CER 合并。 |
| 长音频 SenseVoice CER（字符错误率）和 Whisper-large-v3-turbo CER | `seed_tts_eval` | `<评测目录>/audio_*.wav`、SenseVoiceSmall、Whisper-large-v3-turbo | 只检查台词文本保真；双后端独立报告。 |
| Seed-TTS WavLM-large-SV SIM（说话人验证余弦相似度） | `seed_tts_sim` | 官方中文集的独立 `utt.wav`、每条 meta 的参考音频、`wavlm_large_finetune.pth` | 只作为官方零样本音色克隆基准；不用于长音频最终排名。 |

两个 Conda（Python 环境管理器）环境的 PyTorch、Torchaudio、Hydra 版本不兼容，严禁混装，也不得使用已有的 `audio_eval` 环境替代它们。

## 2. 已验证的环境

### 2.1 `seed_tts_eval`：中文 WER 与长音频 CER

当前已验证版本如下；正式运行前仍必须把完整 `pip freeze` 写入本次结果目录。

| 组件 | 当前版本或要求 |
| --- | --- |
| Python | 3.10.20 |
| PyTorch / Torchaudio | 2.12.0+cu130 / 2.11.0+cu130 |
| CUDA 可用性 | `torch.cuda.is_available()` 为 `True` |
| FunASR / Transformers | 1.3.9 / 5.12.0 |
| Librosa / SoundFile / SciPy | 0.11.0 / 0.14.0 / 1.15.3 |
| 文本与评分工具 | JiWER、`zhconv`、`zhon==2.1.1`、TQDM 4.68.2 |
| 音频命令行工具 | FFmpeg 8.0.1，必须使用该环境中已安装的原生 FFmpeg |

该环境要求 `python -m pip check` 成功，且上述包均可导入。

### 2.2 `seed_tts_sim`：官方 WavLM-large-SV SIM

| 组件 | 当前版本或要求 |
| --- | --- |
| Python | 3.8.20 |
| pip | 固定 24.0；不可升级到 24.1 或更高 |
| PyTorch / Torchaudio | 1.9.0+cu111 / 0.9.0 |
| NumPy / Librosa / SoundFile | 1.22.4 / 0.8.1 / 0.10.3 |
| Fairseq | 当前 Seed-TTS-Eval 仓库内 UniSpeech 源码，提交 `752f4297f090c46bb1a55a1f7439e5944ddefe8d` |
| Fairseq 额外运行时依赖 | `h5py==3.8.0`；Fairseq 顶层导入会无条件加载它 |
| 其他必需包 | Fire 0.4.0、Hydra Core 1.0.7、OmegaConf 2.0.6、S3PRL 0.3.1、SentencePiece、Cython、`libsndfile` |
| CUDA 可用性 | `torch.cuda.is_available()` 为 `True` |

Fairseq 必须以当前 vendored（内置源码副本）安装，不能换成新版 PyPI `fairseq`。安装时固定使用 UniSpeech `setup.py` 自带的 `READTHEDOCS=1` 开关，跳过本机无法链接、且 WavLM-SV 路径不使用的可选原生扩展（`libbleu`、快速索引、`libbase`、`libnat` 和 CUDA 扩展）。这不改变 WavLM 权重、嵌入、余弦相似度或汇总公式。

```bash
conda run --no-capture-output -n seed_tts_sim bash -lc '
  export READTHEDOCS=1
  cd "$HOME/github/seed-tts-eval/thirdparty/UniSpeech/src"
  python -m pip install --timeout 300 --retries 10 --prefer-binary --no-build-isolation -e .
'
```

`seed_tts_sim` 的 `python -m pip check` 仍会报告 S3PRL 的训练、数据集、可视化和 Catalyst 可选依赖未装。这些包不在 `cal_sim.sh` 的运行时路径，不能以该退出码作为失败条件；准入标准是第 6 节的目标入口导入检查。

## 3. 本地资源与目录约定

下列资源均须在本地存在；不要假设 Git 克隆会包含音频、测试集或 `hf-mirror`（本地 Hugging Face 镜像目录）。以下 `$HOME` 是当前用户主目录的可移植写法，不应将展开后的机器专属绝对路径写入仓库配置。

| 资源 | 推荐变量或相对位置 | 必需内容 |
| --- | --- | --- |
| Seed-TTS-Eval 源码 | `$HOME/github/seed-tts-eval` | 评测脚本、UniSpeech 子模块；冻结 Git 提交。 |
| 官方测试集根目录 | `$HOME/github/seed-tts-test-data/seedtts_testset` | `zh/meta.lst`、`zh/hardcase.lst` 及其引用的参考资源。 |
| WavLM-SV 权重 | `$HOME/github/seed-tts-test-data/wavlm_large_finetune.pth` | 官方 `wavlm_large_finetune.pth`。 |
| 中文 WER ASR | `$HOME/hf-mirror/funasr/paraformer-zh` | 至少含 `model.pt`、`config.yaml`、`configuration.json`。 |
| 长音频 SenseVoice ASR | `$HOME/hf-mirror/FunAudioLLM/SenseVoiceSmall` | 至少含 `model.pt`、`config.yaml`、`configuration.json`。 |
| 长音频 Whisper ASR | `$HOME/hf-mirror/openai/whisper-large-v3-turbo` | 至少含 `model.safetensors`、`config.json`、分词器与预处理配置。 |

中文工作流不下载、不加载、也不以 `openai/whisper-large-v3` 替代 Paraformer。Whisper-large-v3-turbo 只用于长音频的第二个 CER 后端，报告中必须明确标注为 turbo，不能称为原版 Whisper-large-v3。

## 4. 不入库的环境变量

在运行 shell 中设置如下变量。`ARNOLD_WORKER_GPU` 必须为实际可用 GPU 数，且不能大于 `torch.cuda.device_count()`；单卡机器通常设为 `1`。

```bash
export SEED_TTS_EVAL_ROOT="$HOME/github/seed-tts-eval"
export SEED_TTS_DATA_ROOT="$HOME/github/seed-tts-test-data/seedtts_testset"
export SEED_TTS_WAVLM_CKPT="$HOME/github/seed-tts-test-data/wavlm_large_finetune.pth"
export SEED_TTS_PARAFORMER_DIR="$HOME/hf-mirror/funasr/paraformer-zh"
export SEED_TTS_SENSEVOICE_DIR="$HOME/hf-mirror/FunAudioLLM/SenseVoiceSmall"
export SEED_TTS_TURBO_DIR="$HOME/hf-mirror/openai/whisper-large-v3-turbo"
export ARNOLD_WORKER_GPU=1
```

变量只在本机 shell、受控的运行脚本或本次结果目录的环境快照中使用；不得提交 `.env`、展开后的个人目录、密钥或大体积音频。

## 5. 执行前必须冻结的源码兼容配置

在正式评分前制作补丁并记录补丁 SHA-256。允许的改动仅为运行兼容，不得改变输入、分片规则、ASR 解码、WavLM 嵌入、相似度计算或汇总公式：

1. 中文 WER 时跳过 `cal_wer.sh` 中无评分作用、却会准备 Whisper-large-v3 的 `prepare_ckpt.py`。
2. `run_wer.py` 必须通过 `AutoModel(model=os.environ["SEED_TTS_PARAFORMER_DIR"], hub="hf")` 加载本地 Paraformer。
3. `cal_wer.sh` 与 `cal_sim.sh` 中的 `sudo split` 必须改为普通用户可执行的 `split`。
4. 保留并冻结 SIM 目录将 `select.py` 改名的补丁，避免遮蔽 Python 标准库 `select`。
5. 固定 Fairseq 的上述 `READTHEDOCS=1` 安装方式及源码提交。

## 6. 非评分环境验证命令

以下命令只导入软件和打印版本，不加载任何 ASR / WavLM 权重，也不执行 `task9.md`。

```bash
conda run --no-capture-output -n seed_tts_eval python -c \
  "import torch, torchaudio, funasr, transformers, librosa, soundfile, scipy, jiwer, zhconv, zhon, tqdm; print(torch.__version__, torchaudio.__version__, torch.cuda.is_available())"

conda run --no-capture-output -n seed_tts_sim bash -lc '
  cd "$HOME/github/seed-tts-eval/thirdparty/UniSpeech/downstreams/speaker_verification"
  python -c "import fairseq, h5py, verification, models.utils; print(\"sim_entrypoint_imports=OK\", fairseq.__version__)"
'
```

当前已验证 SIM 入口输出：

```text
sim_entrypoint_imports=OK 1.0.0a0+752f429
runtime_imports=OK 1.9.0+cu111 0.9.0 1.22.4 3.8.0 1.0.0a0+752f429 True
```

正式运行前还必须写入新建结果目录：两个环境的 `pip freeze`、源码提交、所有测试集/权重/关键配置文件的 SHA-256、GPU/驱动/CUDA 信息、解码参数、文本规范化配置、随机种子和上述补丁 SHA-256。

## 7. 仍不能省略的任务级前提

即使软件环境已满足，以下项目未冻结时也不得宣称正式结果可复现或可横向比较：

- 一个官方中文样本的烟雾检查：本地 Paraformer 与长音频 Whisper-large-v3-turbo 均能加载、独立 `utt.wav` 能生成、WER/SIM 结果文件可写。烟雾检查不能计入模型名次。
- 音频交付硬门槛的格式契约、渠道响度规范和全部阈值。
- 强制对齐的具体工具、发音词典与阈值；闭集角色路由分类器、参考集、校准方式与告警阈值。当前 `task9.md` 尚未为这些项目指定可执行实现时，只能标为“未执行”，不能凭主观推断补分。
- 新建且为空的正式输出目录；只有同一次中断才允许使用 `--resume`。

仓库规则要求同步的 `docs/跨电脑复测指南.md` 当前缺失。本文是 `task9.md` 的环境汇总，不替代该权威复测指南；在正式环境与命令最终定稿前仍需补齐该指南。

## 8. 明确不需要安装或不应纳入自动排序的项目

- 不下载原版 `openai/whisper-large-v3`；它不参与中文 Seed WER，也不由 turbo 替代 Paraformer。
- 不为消除 S3PRL 的 `pip check` 告警而安装完整训练栈（如 Catalyst、Asteroid、Torchvision、Pandas 等）；这些会增加依赖冲突和资源消耗，且不在 WavLM-SV 评分路径。
- 不将长音频 WavLM SIM、SpeechBrain ECAPA SIM、UTMOSv2 或 NISQA-TTS 纳入自动总排名；音色贴合、角色区分、自然度、情绪和长期听感仍须人工盲听。
