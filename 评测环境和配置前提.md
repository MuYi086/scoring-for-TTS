# 长音频公共评测的环境和配置前提

本文汇总执行 [公共评测任务.md](公共评测任务.md) 前所需的软件、隔离环境、本地资源、环境变量和冻结项，供 [`task6.md`](task6.md)、[`task7.md`](task7.md)、[`task8.md`](task8.md) 与 [`task9.md`](task9.md) 共用。它是配置说明，不会执行合成、加载评测权重或计算任何评分。

本文只覆盖长音频的音频交付检查、SenseVoice CER（字符错误率）与 Whisper-large-v3-turbo CER。官方 Seed-TTS 中文 WER（词错误率）和 WavLM-large-SV SIM（说话人相似度）的环境、数据和命令见 [Seed-TTS评测任务.md](Seed-TTS评测任务.md)。

## 1. 评测边界与环境分工

| 工作内容 | 唯一环境 | 输入与本地权重 | 结果边界 |
| --- | --- | --- | --- |
| 长音频 SenseVoice CER 和 Whisper-large-v3-turbo CER | `seed_tts_eval` | `<评测目录>/audio_*.wav`、SenseVoiceSmall、Whisper-large-v3-turbo | 只检查台词文本保真；两个后端独立报告。 |
| 音频交付原始测量 | `seed_tts_eval` | `<评测目录>/audio_*.wav` | 只在格式/响度阈值已冻结时判定通过或失败；否则只报告测量值。 |

`seed_tts_eval` 是已有 Conda（Python 环境管理器）环境的名称；公共评测不因此加载 Seed-TTS-Eval、Paraformer 或 WavLM。不得使用 `audio_eval` 代替它。

## 2. 已验证的环境

当前已验证版本如下；正式运行前仍必须把完整 `pip freeze` 写入本次结果目录。

| 组件 | 当前版本或要求 |
| --- | --- |
| Python | 3.10.20 |
| PyTorch / Torchaudio | 2.12.0+cu130 / 2.11.0+cu130 |
| CUDA 可用性 | `torch.cuda.is_available()` 为 `True` |
| FunASR / Transformers | 1.3.9 / 5.12.0 |
| Librosa / SoundFile / SciPy | 0.11.0 / 0.14.0 / 1.15.3 |
| 文本与评分工具 | JiWER、`zhconv`、`zhon==2.1.1`、TQDM 4.68.2 |
| 音频命令行工具 | FFmpeg 8.0.1，必须使用该环境中已安装的原生 FFmpeg |

该环境要求 `python -m pip check` 成功，且上述包均可导入。

## 3. 本地资源与目录约定

下列资源均须在本地存在；不要假设 Git 克隆会包含待测音频或 `hf-mirror`（本地 Hugging Face 镜像目录）。以下 `$HOME` 是当前用户主目录的可移植写法，不应将展开后的机器专属绝对路径写入仓库配置。

| 资源 | 推荐变量或相对位置 | 必需内容 |
| --- | --- | --- |
| 长音频 SenseVoice ASR | `$HOME/hf-mirror/FunAudioLLM/SenseVoiceSmall` | 至少含 `model.pt`、`config.yaml`、`configuration.json`。 |
| 长音频 Whisper ASR | `$HOME/hf-mirror/openai/whisper-large-v3-turbo` | 至少含 `model.safetensors`、`config.json`、分词器与预处理配置。 |

不下载、不加载、也不以 `openai/whisper-large-v3` 替代 turbo 模型。

## 4. 不入库的环境变量

在运行 shell 中设置如下变量：

```bash
export HF_MIRROR_ROOT="$HOME/hf-mirror"
export HF_HUB_OFFLINE=1
export TRANSFORMERS_OFFLINE=1
export SEED_TTS_SENSEVOICE_DIR="$HOME/hf-mirror/FunAudioLLM/SenseVoiceSmall"
export SEED_TTS_TURBO_DIR="$HOME/hf-mirror/openai/whisper-large-v3-turbo"
```

变量只在本机 shell、受控运行脚本或本次结果目录的环境快照中使用；不得提交 `.env`、展开后的个人目录、密钥或大体积音频。

## 5. 非评分环境验证命令

以下命令只导入软件和打印版本，不加载 ASR 权重，也不执行 task6/task7/task8/task9：

```bash
conda run --no-capture-output -n seed_tts_eval python -c \
  "import torch, torchaudio, funasr, transformers, librosa, soundfile, scipy, jiwer, zhconv, zhon, tqdm; print(torch.__version__, torchaudio.__version__, torch.cuda.is_available())"
```

V6、V7、V8 与 V9 分别必须运行对应的 `check_neutral_evaluation_v6_setup.py --strict-versions`、`check_neutral_evaluation_v7_setup.py --strict-versions`、`check_neutral_evaluation_v8_setup.py --strict-versions` 或 `check_neutral_evaluation_v9_setup.py --strict-versions`，以检查输入哈希、CUDA 和两个 ASR 模型的 revision（修订版本）与哈希。

## 6. 仍不能省略的任务级前提

- 新建且为空的正式输出目录；只有同一次中断才允许使用 `--resume`。
- 本次输入音频、`ai_deal.json`、ASR 权重、关键配置和脚本版本的冻结记录。
- 音频交付硬门槛的格式契约、渠道响度规范和全部阈值。未冻结时，只报告原始测量值。
- 强制对齐的具体工具、发音词典与阈值；闭集角色路由分类器、参考集、校准方式与告警阈值。尚未指定可执行实现时，只能标为“未执行”。

## 7. 明确不需要安装或不应纳入自动排序的项目

- 不下载 Seed-TTS-Eval、Paraformer、WavLM-large-SV 或原版 `openai/whisper-large-v3` 来运行公共长音频评测。
- 不将长音频 WavLM SIM、SpeechBrain ECAPA SIM、UTMOSv2 或 NISQA-TTS 纳入自动总排名；音色贴合、角色区分、自然度、情绪和长期听感仍须人工试听。
