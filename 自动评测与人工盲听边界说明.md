# 自动评测与人工盲听边界说明

## 结论

自动评测保留两类可直接复核的结果：一类是预先冻结阈值的音频交付合规（文件、格式、响度、峰值、静音和掉音等）；另一类是文本保真与结构合规（双 ASR 的 CER、台词覆盖/顺序/重复、强制对齐和冻结发音词典）。这些结果都必须附带原始测量值、阈值和异常位置，才能作为机器结论直接参考。

SenseVoice CER（字符错误率）与 Whisper CER 回答的是“这条成品是否存在错字、漏字、多字或重复”，而不是音色是否像目标角色，也不回答是否自然、情绪是否贴合。标点停顿和语速只有在按预先冻结的文本/时间阈值检查时才属于自动合规；停顿是否自然、有戏，仍需人耳判断。

WavLM SIM、SpeechBrain ECAPA SIM、UTMOSv2 与 NISQA-TTS 不再作为自动评测机制，也不再用于生成自动综合分或自动总排名。这四项指标应只作为后续人工盲听的定位线索，而不是机器定版依据。

## 三份原报告应如何使用

| 报告或检查 | 可由脚本直接参考的内容 | 是否可以直接按机器结果判定 | 后续处理 |
| --- | --- | --- | --- |
| 音频交付硬门槛 | 文件可解码性、格式、时长、削波/真峰值、直流偏置、响度、静音、掉音和截断。 | 可以，但仅按评测前冻结的格式契约、渠道规范和阈值判定通过或失败；没有冻结阈值时只报告测量值。 | 修复失败音频后复测，不据此推断自然度或音色好坏。 |
| 台词完整性与强制对齐 | 台词覆盖、缺失、插入、重复、顺序、可对齐覆盖率、冻结读法以及阈值化的停顿/语速异常。 | 可以，作为文本和时间合规的通过/失败或异常清单；需要保留原始位置证据。 | 对 ASR 或对齐无法确定的条目逐条复核。 |
| SenseVoice CER 与 Whisper CER | 台词正确性风险：错字、漏字、多字、重复，以及需要人工核对的转写位置。 | 两个 ASR（自动语音识别）后端均支持时，可作为台词正确性的自动筛选依据；不能替代有争议条目的逐字核稿。 | 对两个后端分歧大的模型查看完整转写并人工核对。 |
| 闭集角色路由告警 | 疑似串角或错路由的片段。 | 仅在独立校准的角色分类器、参考集和阈值已冻结时，可按告警规则判定；不是音色排名。 | 试听确认告警片段；没有校准分类器时不运行。 |
| WavLM SIM 与 SpeechBrain ECAPA SIM | 可能存在的角色音色短板，以及应优先试听的角色和模型。 | 不可以。 | 自动机制移除；对齐同句后，由人工盲听重排音色贴合、角色区分度与稳定性。 |
| UTMOSv2 与 NISQA-TTS | 可能的伪影、清晰度或混音异常线索。 | 不可以。 | 自动机制移除；由人工连续试听重排自然度、情绪、停顿与听觉疲劳。 |


### 音频交付、结构和文本时间：可作自动验收

文件解码、格式、时长、响度、真峰值、静音和掉音是交付约束，而不是审美预测。只要在评测前冻结目标格式与渠道规范，脚本可以直接给出通过/失败及证据。不同发布渠道的响度目标并不相同，不能在评测完成后临时选择有利阈值。

文本覆盖、缺失、插入、重复、顺序，以及使用冻结发音词典的强制对齐覆盖率，也可以作为可复查的机器结果。标点停顿、语速与异常静音仅能按预声明的时间阈值做合规检查；不能由此推断叙事节奏或情绪表达优秀。

闭集角色分类只适合发现“应为甲角色的片段疑似被路由成乙角色”的异常。它必须使用独立校准的参考集与阈值；输出为告警和复核入口，不是角色音色质量分，更不能替代人工排序。

### CER：可作自动筛选，但只限台词正确性

CER 使用实际合成输入 `longAudioTestV9/ai_deal.json` 的 77 段台词、1,505 个规范化字符作为参考，而没有误用与实际输入不完全相同的 `text.md`。这使它适合发现全文漏读、重复和明显错读。

双 ASR 在 V9 的名次相关为 0.321，说明不能把任一单后端的名次视为绝对事实。正确用法如下：

- 两个后端均靠前或均靠后时，可直接用作台词正确性筛选信号；
- 两个后端分歧大时，查看 `per_audio.jsonl` 的完整转写，并按对应音频人工核对；
- CER 不评价音色、情绪、停顿、自然度或角色映射；背景音乐和音效也可能影响 ASR 结果。

### SIM：必须由人耳确定真实排序

SIM（说话人嵌入余弦相似度）只反映两个嵌入向量的接近程度，不是“同一人概率”，更不是人耳的角色音色判断。V9 中 WavLM 的第一名是 MOSS-TTS-Local-Transformer-v1.5，而 ECAPA 的第一名是 OmniVoice；两个后端名次相关只有 0.214。

此外，SIM 依赖 Whisper 自动时间戳定位片段，背景层、音效、短台词和对齐误差都会改变结果。因此不能用其机器名次确定角色音色胜负。人工试听应使用“参考音频 + 各模型同一句台词”的盲评方式，分别评分：

- 音色身份贴合；
- 角色之间是否容易区分；
- 同一角色在长段中的稳定性。

### 自然度：必须由人耳确定真实排序

UTMOSv2 与 NISQA-TTS 是无参考质量预测器，不是针对本批中文多角色、带背景层有声书校准的真人 MOS（平均意见分）。V9 中 UTMOSv2 第一名为 Qwen3-TTS-12Hz-1.7B-Base，NISQA-TTS 第一名为 OmniVoice，两个后端名次相关为 -0.286。

它们不能可靠评判情绪是否符合剧情、停顿是否有戏、角色切换、长时间疲劳感及背景层下的自然度。因此自然听感的真实排序必须通过匿名、随机顺序的人工试听完成；至少覆盖五个角色和 10–15 分钟连续片段。

## 综合报告的边界

`小说转有声TTS_V5综合评价报告.md` 的名次加权方法避免了直接平均跨量纲原始数值，但其输入包含相互分歧的 SIM 与自然度名次。它只能用来安排试听优先级，不得作为自动定版或最终生产排名。

移除自动综合排名后，最终生产选择应在以下人工证据齐备后另行决定：

1. CER 标出的台词错误已经逐项核对；
2. 五个角色完成参考音频对照盲听；
3. 完成连续长段试听，记录自然度、情绪、伪影和疲劳感；
4. 如需综合分，使用预先声明的人工评分表，并保留每位评审的原始记录。

## 自动输出范围

自动阶段输出逐模型的原始测量值、冻结阈值、通过/失败、异常位置和可复核证据，并汇总为：

- `SenseVoice_CER&Whisper_CER_V9评价报告.md`；
- `音频交付与文本一致性_V9自动检查报告.md`。

该阶段不输出 WavLM/ECAPA 报告、UTMOSv2/NISQA 报告或自动综合排名。
