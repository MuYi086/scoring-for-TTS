# 公共评测任务当前阻塞清单

目前**没有缺少的软件包**阻塞中文 Seed-TTS-Eval 或长音频自动 CER（字符错误率）运行。已从清单移除：本地 Paraformer、SenseVoiceSmall、Whisper-large-v3-turbo、官方测试集、WavLM-large-SV 权重、`seed_tts_eval` 基础依赖与 FFmpeg，以及 `seed_tts_sim` 的 Python 3.8、PyTorch 1.9.0 + CUDA 11.1、Torchaudio 0.9.0、pip 24.0、NumPy、SciPy、Librosa、SoundFile Python 包、原生 libsndfile、Fire、Hydra Core、OmegaConf、Cython、S3PRL 接口、SentencePiece、Conda 编译器包、`libxcrypt`、`h5py==3.8.0` 和当前 Seed-TTS-Eval 仓库内的 Fairseq。

> 环境边界：`seed_tts_eval` 运行中文 Seed WER（词错误率）与长音频 SenseVoice / Whisper-large-v3-turbo CER；`seed_tts_sim` 只运行官方 WavLM-large-SV SIM（说话人余弦相似度）。严禁混装。

## 已验证的 SIM 运行时

Fairseq 使用当前 UniSpeech vendored（内置源码副本）源码，安装时固定 `READTHEDOCS=1`，以跳过 WavLM-SV 路径不使用的可选原生扩展。该方式用于规避本机 Conda 交叉链接器对 `/lib64/libm.so.6` 的错误依赖；它不改变 WavLM 权重、嵌入计算、余弦相似度或汇总公式。

已完成且通过的非评分导入验证：

```text
sim_entrypoint_imports=OK 1.0.0a0+752f429
runtime_imports=OK 1.9.0+cu111 0.9.0 1.22.4 3.8.0 1.0.0a0+752f429 True
```

其中依次包含 PyTorch、Torchaudio、NumPy、`h5py`、Fairseq 与 CUDA 可用性。`h5py` 是 Fairseq 顶层导入无条件加载音频数据集所需的运行时依赖；它不是 S3PRL 的可忽略训练依赖。

## `pip check` 的预期提示

S3PRL 0.3.1 只安装了 `cal_sim.sh` 实际使用的 `UpstreamBase` 接口；其训练、数据集、可视化与 Catalyst 依赖未安装，故 `python -m pip check` 会列出这些未安装包并以非零状态结束。这是已知且预期的记录项，不是任务阻塞；准入标准是上节的目标运行时导入，而不是 `pip check` 的退出码。

`omegaconf==2.0.6` 的弃用提示同样只说明该旧版包的元数据格式已过时；本环境固定 `pip==24.0` 来兼容官方旧 Fairseq 依赖，不能将 pip 升级到 24.1 或更高。

## 尚待处理的非安装阻塞项

以下不是下载或安装软件，用户无需自行处理；它们会在后续不执行正式评测的配置阶段完成并冻结：

- Seed-TTS-Eval 中文 WER 源码兼容补丁：跳过无用的 Whisper-large-v3 预加载、显式加载本地 `funasr/paraformer-zh`、移除两份脚本的 `sudo split`，并冻结补丁 SHA-256。
- 已存在的 SIM `select.py` 改名补丁需要与上述补丁一起固定、校验，避免遮蔽 Python 标准库。
- 逐个本地 ASR 权重及配置的 SHA-256、环境 `pip freeze`、GPU / CUDA 信息需要在后续烟雾检查结果目录中冻结。
- `docs/跨电脑复测指南.md` 当前缺失；按仓库规则，评测环境和复测命令最终定稿前需要补齐并同步该指南、根 `README.md` 与 `tts-bench/README.md`。

本清单不要求再安装任何软件，也不要求执行 `task9.md`、加载评测权重或计算评分。
