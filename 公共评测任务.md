# 公共评测任务

本文定义 `task6.md`、`task7.md`、`task8.md` 与 `task9.md` 共用的**长音频**手动评测任务、自动检查边界和执行前置条件。调用任务只负责提供本次评测目录、人物/参考音频、`text.md`、待测 `audio_*.wav`、版本标识和输出目录；不得在任务文件中复制或改写本文规则。


## 1. 调用约定

每个调用任务必须显式提供以下配置：

| 配置项 | 含义 |
| --- | --- |
| `<评测目录>` | 本次版本的 `longAudioTestV*` 目录，内含 `text.md`、角色参考音频与待测 `audio_*.wav`。 |
| `<版本标识>` | 用于报告文件名区分的版本，例如 `V8` 或 `V9`。 |
| `<输出目录>` | `<评测目录>/评测结果`；正式评分必须新建且为空，只有同一次中断才可使用 `--resume`。 |
| `<实际台词串>` | 以本次 `text.md` 中实际参与合成、且顺序固定的全部台词拼接结果为准；不能复用其他版本的台词数或文本。 |

每次只处理一个模型对应的 `audio_*.wav`。所有原始值、冻结阈值、通过/失败状态、异常位置和可复核证据均输出到 `<输出目录>`。

### 1.1 多模型长文本克隆的共享分段协议

当同一份长文本要比较两个或以上的克隆模型时，模型间不能各自按不同字数、不同标点或不同停顿策略分段。必须在加载第一个模型前生成并冻结一份共享分段清单，所有模型都按该清单逐段合成、按同一边界停顿拼接；否则文本上下文、单段负载和拼接韵律同时变化，不能把结果视为公平比较。

- 清单至少记录：源文本与参考音频哈希、参考文案、文本规范化规则、参考语速、目标/最大片段时长、每段台词与哈希、顺序、估算时长、边界类型和边界后的静音时长。
- 片段优先在完整句、段落边界处断开；单句超过最大时长预算时，才可按次级标点或显式的字符预算硬切。禁止把固定“最多 N 字”当作不随角色语速变化的通用标准。
- 目标和最大片段时长必须在本次机器可读配置中冻结。它们可由参考音频真实语速换算为字符预算，但不得在模型之间变化。
- 需要前后文的后端，必须冻结上下文 API（应用程序接口）参数；没有该能力的后端不得把前后台词拼入待朗读文本。上下文只能作为可复核元数据，不能污染 CER 的唯一参考台词。
- 每种边界的拼接静音必须在清单中明确并由所有模型一致执行。模型原生采样率、位深或时长差异应记录为交付原始测量，除非交付契约已要求统一转换。

## 2. 手动执行前置条件

本公共任务不在文档修改时执行任何评测。预检失败时停止，不得用另一版本的长音频、替代 ASR 权重或不同版本的配置继续评分。

- 长音频的 SenseVoice CER（字符错误率）与 Whisper-large-v3-turbo CER 都在Conda（Python 环境管理器）环境运行
- 详细的软件版本、本地资源、环境变量和非评分导入检查见 [评测环境和配置前提.md](评测环境和配置前提.md)。`python -m pip check` 必须成功。
- 本项目只评测中文。正式运行必须冻结 SenseVoiceSmall、Whisper-large-v3-turbo、输入音频和文本的哈希，记录 `pip freeze`、GPU/驱动/CUDA、解码参数、文本规范化配置和随机种子；记录只写入本次结果目录，不写入仓库配置。
- 不下载、不加载、也不以原版 `openai/whisper-large-v3` 替代 Whisper-large-v3-turbo。后者在报告中必须完整标注为 turbo。
- 多模型长文本任务还必须在评测结果中冻结共享分段清单的完整快照和哈希，并验证其源文本哈希与本次 `<实际台词串>` 一致。

## 3. 可直接参考的自动评测机制

所有数值阈值、交付格式和目标渠道响度规范必须在实际评测前冻结到机器可读配置；没有冻结阈值时，只报告原始测量值和异常位置，不得自行判定通过、失败或优劣。

### 3.1 音频交付硬门槛

- 检查文件可解码性、非空性、哈希、采样率、声道数、位深与时长是否符合冻结的交付契约。
- 按冻结阈值检查削波、最大真峰值、直流偏置、前后静音、非预期长静音、掉音和突然截断。
- 按目标发布渠道的冻结规范检查集成响度、短时响度、响度范围和最大真峰值。

### 3.2 台词与结构完整性

- 对每个模型的完整合成音频，以 `<实际台词串>` 为唯一文本参考，分别计算 SenseVoice CER 与本地 Whisper-large-v3-turbo CER。
- 核对台词覆盖、缺失、插入、重复和顺序错误，保留完整转写、错误位置和对应原始台词。双 ASR 后端分别报告和排名，不得平均为综合分。
- ASR 输出清理规则必须冻结并可复核。仅可移除后端明确输出、且不会被音频朗读的控制标记（例如 SenseVoice 的语言、情绪或事件标签）；必须保留逐段原始转写字段。不得对实际台词进行纠错、补字、改写或人工后处理后再计算 CER。
- CER 仅衡量文本保真，不表示音频质量、自然度或角色表现。Whisper-large-v3-turbo CER 不得标作原版 Whisper-large-v3 CER。

### 3.3 文本时间与读法合规

- 使用冻结发音词典的强制对齐，检查字符或词级可对齐覆盖率、未对齐比例、台词/角色覆盖和文本顺序。
- 按冻结阈值检查标点停顿时长、语速和异常静音；中文专有名词、数字、日期和英文读法以冻结词典为准。

### 3.4 角色路由告警（非音色评分）

- 仅在闭集角色分类器、参考音频集、校准方式与阈值均已冻结时，检查每段的目标角色 Top-1 与对其他候选角色的置信差距，输出“疑似串角/错路由”告警。
- 该检查不是音色质量分，不用于自动排名或综合分。未配置校准分类器时，报告必须标明“未执行”，不得用其他相似度分数补位。

## 4. 公共报告与人工边界

所有模型完成后，在 `<输出目录>` 生成：

- `SenseVoice_CER&Whisper-large-v3-turbo_CER_<版本标识>评价报告.md`：双 ASR 的全文 CER、完整转写、台词错误位置、双后端名次和分歧项。
- `音频交付与文本一致性_<版本标识>自动检查报告.md`：交付硬门槛、台词/结构完整性、强制对齐/读法合规和角色路由告警的逐模型结果。

多模型长文本报告还必须说明共享分段清单的段数、时长预算、边界停顿规则和清单哈希，并链接或包含原始结果中的清单快照；这属于比较条件证据，而不是音质评分。

音色贴合、角色区分度、自然度、情绪、表演性停顿、伪影主观感受和长时间听觉疲劳由用户人工试听后直接判断；不要求盲化播放列表、评分表或独立的 `listener-review` 目录。具体边界见 [自动评测与人工盲听边界说明.md](自动评测与人工盲听边界说明.md)。

> Task 9 的 V9 受限入口位于 `task-runner/task9/`，只实现本文的长音频交付原始测量与双 CER；它不加载旧版六后端指标，也不生成跨量纲综合分。
