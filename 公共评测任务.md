# 公共评测任务

本文是 `task6.md`、`task7.md`、`task8.md` 与 `task9.md` 的唯一公共执行规范，覆盖两阶段顺序、受限入口、合成证据、环境预检、双 ASR（自动语音识别）评测与报告。各任务文件只保留本次固定输入和最终成品；先读取对应任务文件，再按本文选择该任务的执行绑定。不得将 `tts-bench` 的通用短样本评价器用于这些长音频任务。

本文中的命令用于生产执行说明，本次文档优化不执行任何合成、预检、评测或报告生成命令。

## 1. 共同流程与不可变边界

每个任务都严格按以下两阶段进行：

1. 使用 `indextts2`（IndexTTS2）和 `voxcpm2`（VoxCPM2）以固定顺序串行合成两条候选成品，并生成共享分段清单和逐段合成证据。
2. 对两条成品先做只读预检，再用两个 ASR 后端分别完成严格评测和报告导出。

`audio_indextts2.wav` 与 `audio_voxcpm2.wav` 是第一阶段输出，不是任务开始前的外部前置条件。模型集合必须且只能为上述两项，顺序始终为 IndexTTS2、VoxCPM2；不得加入 Qwen3-TTS、MiMo、dots.tts 或任何其他候选，也不得用历史成品替代本轮产物。

每个任务只能使用其任务文件“固定输入与最终成品”表中的 `text.md` 全文、参考音频、参考文案与音色说明。禁止改用 `ai_deal.json`、旧字符统计、历史报告、其他版本文本或音频。成品、模型目录、源码目录、运行缓存和本机计划均不提交；不得在计划、命令行或仓库配置中写入 API 密钥。

### 1.1 运行器依赖边界

`task-runner/task*/` 是唯一权威入口，包含冻结契约、合成器、预检器、评测器和报告生成器。Task 6/7 的合成包装器复用 Task 8 合成器，而 Task 6–8 的本机计划又调用 Task 9 的分段、证据和模型适配脚本；因此回测任一 Task 6–9 时必须保留整个 `task-runner/` 目录。

```text
Task 6 / Task 7 合成包装器 → Task 8 共享合成器 → Task 9 分段、证据与模型适配脚本
Task 8 本机计划 ───────────────────────────────→ Task 9 分段、证据与模型适配脚本
Task 9 编排器 ─────────────────────────────────→ Task 9 分段、证据与模型适配脚本
```

## 2. 任务执行绑定

下表集中保存路径、入口与结果目录命名。其余章节的尖括号占位符均取自本表和对应任务文件，不能自行替换为其他任务的路径。

| Task | `<任务目录>` | `<版本标识>` | `<评测运行目录>` | `<预检器>` | `<评测器>` | `<报告生成器>` | `<结果目录前缀>` |
| --- | --- | --- | --- | --- | --- | --- | --- |
| 6 | `longAudioTestV6` | `V6` | `task-runner/task6` | `check_task6_evaluation_setup.py` | `run_task6_evaluation.py` | `generate_task6_reports.py` | `task-V6` |
| 7 | `longAudioTestV7` | `V7` | `task-runner/task7` | `check_task7_evaluation_setup.py` | `run_task7_evaluation.py` | `generate_task7_reports.py` | `task-V7` |
| 8 | `longAudioTestV8` | `V8` | `task-runner/task8` | `check_task8_evaluation_setup.py` | `run_task8_evaluation.py` | `generate_task8_reports.py` | `task-V8` |
| 9 | `longAudioTestV9` | `V9` | `task-runner/task9` | `check_task9_evaluation_setup.py` | `run_task9_evaluation.py` | `generate_task9_reports.py` | `task9-v2` |

各任务的 `<输出目录>` 固定为 `<任务目录>/评测结果`；正式评测的每次 `<结果目录>` 必须是此前从未使用过的新目录。

### 2.1 Task 6–8：计划型合成入口

Task 6–8 均从仓库模板复制被忽略的本机 JSON（JavaScript 对象表示法）计划。计划中的模型目录、源码目录和 Conda（Python 环境管理器）环境必须按本机实际情况填写，且不得写入密钥。命令必须保持 JSON 字符串数组形式，不经 shell 解释。

| Task | 计划模板 | 本机计划 | 无模型预检 | 实际合成 |
| --- | --- | --- | --- | --- |
| 6 | `task-runner/task6/synthesis-plan.example.json` | `longAudioTestV6/.task6-synthesis-plan.json` | `python task-runner/task6/run_task6_synthesis.py --dry-run` | `python task-runner/task6/run_task6_synthesis.py` |
| 7 | `task-runner/task7/synthesis-plan.example.json` | `longAudioTestV7/.task7-synthesis-plan.json` | `python task-runner/task7/run_task7_synthesis.py --dry-run` | `python task-runner/task7/run_task7_synthesis.py` |
| 8 | `task-runner/task8/synthesis-plan.example.json` | `longAudioTestV8/.task8-synthesis-plan.json` | `python task-runner/task8/run_task8_synthesis.py --plan longAudioTestV8/.task8-synthesis-plan.json --dry-run` | `python task-runner/task8/run_task8_synthesis.py --plan longAudioTestV8/.task8-synthesis-plan.json` |

先复制对应模板，编辑本机路径，再运行表中的无模型预检；预检只校验计划、固定输入与展开后的命令，不写入音频。确认无误后才运行“实际合成”列。已有同名候选默认拒绝覆盖；只有明确需要重新合成时才增加 `--overwrite`，并为后续评测创建新的结果目录。

四个合成入口都支持用 `--models <模型标识> ...` 限定本次重跑的候选；这只适合补跑或诊断。正式评测前仍须确认两条当前成品均存在、使用同一共享清单且均有哈希绑定证据；不得在已冻结的结果目录中替换任一候选。

```bash
# 以 Task 6 为例；Task 7/8 使用上表对应的两条路径。
cp task-runner/task6/synthesis-plan.example.json \
  longAudioTestV6/.task6-synthesis-plan.json
```

计划中 `models` 必须且只能按 `indextts2`、`voxcpm2` 排列。IndexTTS2 只能通过独立 `--emo-text {voice_description}` 接收风格说明；VoxCPM2 只可接收 `{reference_audio}`、`{reference_transcript}` 与正文片段，严禁加入 `--style-prompt`，否则本地脚本会把它拼入正文并朗读。

### 2.2 Task 9：直接编排合成入口

Task 9 不使用本机计划文件。模型目录、IndexTTS2 源码目录和本地镜像通过环境变量或同名命令参数提供，绝不写入仓库。以下两条命令保留既有语义：先执行无模型预检；仅在明确允许覆盖现有两条目标成品时执行带 `--overwrite` 的实际合成。

```bash
# 只校验路径、环境与将要生成的共享清单；不加载模型、不写入音频。
HF_MIRROR_ROOT=/path/to/hf-mirror \
INDEXTTS_CODE_PATH=/path/to/index-tts \
python task-runner/task9/run_task9_clone.py --dry-run

# 生产操作：会覆盖已有 Task 9 目标音频，须由执行人明确确认后才运行。
HF_MIRROR_ROOT=/path/to/hf-mirror \
INDEXTTS_CODE_PATH=/path/to/index-tts \
python task-runner/task9/run_task9_clone.py --overwrite
```

### 2.3 共享分段、成品与证据协议

所有任务都先按参考音频的真实语速生成一份共享语义分段清单，默认目标片段时长为 25 秒、最大时长为 35 秒。优先在完整句和段落边界切分；只有单句超出最大时长预算时，才按次级标点或字符预算拆分。不得把“最多 80 字”当作固定规则，也不得把相邻上下文、参考文案或音色说明注入待朗读文本。

两个模型必须读取同一份清单，并以相同规则拼接：强制断开 250ms、句末 500ms、段落末 750ms、末段 0ms。清单记录原文/参考音频哈希、参考语速、时长预算、每段文本哈希、顺序、估算时长、边界类型与停顿；其逐段证据记录片段 WAV（波形音频）哈希、片段在成品中的时间位置和最终 WAV 哈希。

每条候选必须在可解码、非空后才成为可评测成品。不得在合成后手工重编码、响度处理、移动或替换候选 WAV，也不得手工编辑、移动或提交共享清单与证据目录；任一变化都会使哈希绑定失效，必须重新合成并使用新的结果目录评测。

## 3. 正式评测前置条件

阶段一完成两条候选及其逐段证据后，才可以进入正式评分。先运行任务绑定的只读预检；预检必须确认：

- `<实际台词串>`、参考音频、两条候选成品、共享清单和逐段证据均存在、非空、可解码且哈希一致；
- 本地 `SenseVoiceSmall` 与 `Whisper-large-v3-turbo` 的冻结 revision（修订版本）可从 `HF_MIRROR_ROOT` 离线加载；不得联网下载，也不得用原版 `openai/whisper-large-v3` 替代 turbo；
- CUDA（统一计算设备架构）可用、`python -m pip check` 成功，且 `pypinyin==0.55.0` 与冻结配置一致；
- 台词、参考音频、候选成品和共享清单哈希均记录到本次原始结果，同时记录 `pip freeze`、GPU/驱动/CUDA、解码参数、文本规范化配置和环境变量快照。

仓库不会包含被 `.gitignore` 忽略的 `audio_*.wav`、本机计划或 `hf-mirror`。缺少这些本地资产是预检失败，不能改用另一版本的音频、台词或 ASR 权重继续评分。

## 4. 公共评测执行顺序

先从第 2 节选择目标 Task 的绑定，并从任务文件读取 `<实际台词串>`、参考音频与候选路径。以下命令中的尖括号占位符必须替换为该绑定的值或本机 `HF_MIRROR_ROOT`，不得写入仓库配置。

```bash
# 1. 只读预检。
HF_MIRROR_ROOT=/path/to/hf-mirror \
conda run --no-capture-output -n audio_eval \
  python <评测运行目录>/<预检器>

# 2. 新建本次原始结果目录。Task 9 使用 task9-v2；其余任务使用 task-V*。
RESULT_DIR="<任务目录>/评测结果/<结果目录前缀>-$(date -u +%Y%m%dT%H%M%SZ)"

# 3. 首个模型创建结果目录。
HF_MIRROR_ROOT=/path/to/hf-mirror \
conda run --no-capture-output -n audio_eval \
  python <评测运行目录>/<评测器> \
  --model-id indextts2 --output-dir "$RESULT_DIR" --strict

# 4. 第二个模型只对同一次未完成运行使用 --resume。
HF_MIRROR_ROOT=/path/to/hf-mirror \
conda run --no-capture-output -n audio_eval \
  python <评测运行目录>/<评测器> \
  --model-id voxcpm2 --output-dir "$RESULT_DIR" --resume --strict

# 5. 两个模型均完成后生成报告。
python <评测运行目录>/<报告生成器> \
  --results-dir "$RESULT_DIR" --reports-dir <任务目录>/评测结果
```

每次进程只处理一个模型成品。首次运行会冻结评测契约、实际台词、参考音频、候选成品、共享清单及其 SHA-256（安全散列算法）哈希；同一结果目录续跑时任一输入或契约变化都必须停止并新建目录。只有继续同一次未完成运行时才可使用 `--resume`；中途失败不得提前生成不完整名次。

## 5. 固定评测规则与报告边界

每条模型成品都必须以 `<实际台词串>` 为唯一参考，分别运行 SenseVoice CER（字符错误率）和 Whisper-large-v3-turbo CER。

- 两个后端分别报告原始值和独立名次，禁止平均为综合分。
- SenseVoice 的语言、情绪和事件控制标记（如 `<|zh|>`、`<|HAPPY|>`）仅可从 CER 输入移除；逐段 `raw_transcription`（原始转写）必须保留。不得纠错、补字、改写或人工后处理转写后再计算 CER。
- 严格汉字 CER 是唯一正式文本差异测量；带声调拼音 CER 只提示严格差异可能为同音字，不能取代严格 CER，也不能证明没有错读。
- ASR 健康门控应检查转写长度比例、连续删除和双后端分歧。健康门控不通过时，必须保留原始 CER、转写和错误位置，但该后端不得参与名次。
- CER 只衡量文本保真，不表示音色贴合、自然度、情绪或角色表现。

两个 ASR 均直接读取与最终 WAV 哈希绑定的逐段合成证据，按冻结的共享语义段独立转写并汇总全文 CER；禁止改用固定时间窗口或将整条长音频单次送入 ASR。

每条成品还必须记录可解码性、非空性、哈希、采样率、声道数、位深、时长、削波比例、采样峰值、直流偏置、首尾静音和长静音位置。当前契约未冻结目标渠道格式、响度、真峰值、静音或时长阈值，因此只报告原始测量值和异常位置，不自行判定通过、失败或优劣。强制对齐、读法词典校准和角色路由分类器同样未冻结，报告必须明确标记“未执行”。

不得加载长音频 WavLM SIM、SpeechBrain ECAPA SIM、UTMOSv2、NISQA-TTS 或自动综合排名，更不能把不同量纲的原始数值直接平均。

所有模型完成后，在 `<任务目录>/评测结果` 生成：

- `SenseVoice_CER&Whisper-large-v3-turbo_CER_<版本标识>评价报告.md`：双 ASR 的严格/拼音 CER、完整转写、字符错误位置、独立名次、分歧与健康状态。
- `音频交付与文本一致性_<版本标识>自动检查报告.md`：交付原始测量、台词/结构一致性及强制对齐、读法合规、角色路由等未执行项。

音色贴合、角色区分度、自然度、情绪、表演性停顿、伪影主观感受和长时间听觉疲劳由人工试听判断。自动结果的适用边界见[自动评测与人工盲听边界说明.md](自动评测与人工盲听边界说明.md)。

## 附录：Task 9 v1 历史执行记录（不可比较）

以下记录采用已废弃的固定 30 秒窗口转写，窗口边界的漏转写会放大后续差异；它不能作为当前 `task9-v2` 的模型错误率、名次或基线。重新合成后，必须按第 4 节使用新的结果目录完成 v2 评测，才可比较当前严格 CER、拼音辅助指标、ASR 健康状态、逐段转写和人工试听。

| 模型 | SenseVoice CER | Whisper-large-v3-turbo CER |
| --- | ---: | ---: |
| IndexTTS2 | 0.036018 | 0.187950 |
| VoxCPM2 | 0.098887 | 0.178782 |
