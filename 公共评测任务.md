# 公共评测任务

本文定义 `task6.md`、`task7.md`、`task8.md` 与 `task9.md` 共用的长音频评测规则、执行顺序和报告边界。调用任务只提供本次目录、参考音频、实际合成文本、候选成品、版本标识和各自的受限入口；不得在任务文件中复制或改变本文规则。

## 1. 调用约定

每个调用任务必须显式提供以下配置：

| 配置项 | 含义 |
| --- | --- |
| `<评测目录>` | 本次版本的 `longAudioTestV*` 目录，内含 `text.md`、参考音频和待测成品。 |
| `<版本标识>` | 用于报告名区分的版本，例如 `V8` 或 `V9`。 |
| `<实际台词串>` | 本次 `text.md` 中实际参与合成、顺序固定的全部台词；不能复用其他版本的台词数、`ai_deal.json` 或文本。 |
| `<评测运行目录>` | 该任务专用的 `task-runner/task*/` 目录，包含冻结契约、预检器、评测器和报告生成器。 |
| `<输出目录>` | `<评测目录>/评测结果` 下本次新建的原始结果目录；只有继续同一次未完成运行时才可使用 `--resume`。 |

每次进程只处理一个模型成品。首次运行会冻结评测契约、实际台词、参考音频、候选成品及其 SHA-256（安全散列算法）哈希；同一结果目录续跑时，任何上述输入或契约发生变化都必须停止并新建结果目录。

## 2. 固定评测规则

### 2.1 评测前置条件

正式评分前必须先运行调用任务提供的只读预检。预检必须确认：

- `<实际台词串>`、参考音频和全部候选成品存在、非空且可解码；
- 本地 `SenseVoiceSmall` 与 `Whisper-large-v3-turbo` 的冻结 revision（修订版本）可从 `HF_MIRROR_ROOT` 离线加载；不得联网下载，也不得用原版 `openai/whisper-large-v3` 替代 turbo；
- CUDA（统一计算设备架构）可用、`python -m pip check` 成功，且 `pypinyin==0.55.0` 与冻结配置一致；
- 台词、参考音频和成品哈希均在本次原始结果中记录，同时记录 `pip freeze`、GPU/驱动/CUDA、解码参数、文本规范化配置和环境变量快照。

仓库不会包含被 `.gitignore` 忽略的 `audio_*.wav` 或 `hf-mirror`。缺少它们是预检失败，不得改用另一版本的音频、台词或 ASR 权重继续评分。

### 2.2 双 ASR 文本保真

每条模型成品都必须以 `<实际台词串>` 为唯一参考，分别运行 SenseVoice CER（字符错误率）和 Whisper-large-v3-turbo CER。

- 两个后端分别报告原始值和独立名次，禁止平均为综合分。
- SenseVoice 的语言、情绪和事件控制标记（如 `<|zh|>`、`<|HAPPY|>`）仅可从 CER 输入移除；逐段或整条的 `raw_transcription`（原始转写）必须保留。不得纠错、补字、改写或人工后处理转写后再计算 CER。
- 严格汉字 CER 是唯一正式文本差异测量；带声调拼音 CER 只提示严格差异可能为同音字，不能取代严格 CER，也不能证明没有错读。
- ASR 健康门控应检查转写长度比例、连续删除和双后端分歧。健康门控不通过时，必须保留原始 CER、转写和错误位置，但该后端不得参与名次。
- CER 只衡量文本保真，不表示音色贴合、自然度、情绪或角色表现。

Task 6、Task 7、Task 8 与 Task 9 均使用与最终 WAV 哈希绑定的逐段合成证据，按冻结的共享语义段独立转写；固定时间窗口和整条长音频单次转写都不得作为 CER 评测单元。原始结果和报告必须如实标注此证据链路。

### 2.3 音频交付与未执行项

每条成品必须记录可解码性、非空性、哈希、采样率、声道数、位深、时长、削波比例、采样峰值、直流偏置、首尾静音和长静音位置。

当前契约尚未冻结目标渠道格式、响度、真峰值、静音或时长阈值，因此只报告原始测量值和异常位置，不自行判定通过、失败或优劣。强制对齐、读法词典校准和角色路由分类器同样未冻结，报告必须明确标记“未执行”。

不加载长音频 WavLM SIM、SpeechBrain ECAPA SIM、UTMOSv2、NISQA-TTS 或自动综合排名；更不能把不同量纲的原始数值直接平均。

## 3. 公共执行顺序

先在调用任务中确认 `<评测运行目录>`、模型标识和成品文件名。以下命令中的占位符必须替换为调用任务列出的相对路径或本机 `HF_MIRROR_ROOT`，不得写入仓库配置。

```bash
HF_MIRROR_ROOT=/path/to/hf-mirror \
conda run --no-capture-output -n audio_eval \
  python <评测运行目录>/<预检器>
```

预检通过后，为本次完整评测创建从未使用过的目录。首次评测一个模型；其余模型仅对同一目录增加 `--resume`：

```bash
RESULT_DIR="<输出目录>/task-<版本标识>-$(date -u +%Y%m%dT%H%M%SZ)"

HF_MIRROR_ROOT=/path/to/hf-mirror \
conda run --no-capture-output -n audio_eval \
  python <评测运行目录>/<评测器> \
  --model-id <第一个模型标识> --output-dir "$RESULT_DIR" --strict

HF_MIRROR_ROOT=/path/to/hf-mirror \
conda run --no-capture-output -n audio_eval \
  python <评测运行目录>/<评测器> \
  --model-id <下一个模型标识> --output-dir "$RESULT_DIR" --resume --strict

python <评测运行目录>/<报告生成器> \
  --results-dir "$RESULT_DIR" --reports-dir <评测目录>/评测结果
```

只有全部候选模型都完成后，报告生成器才会写出两份公共报告；中途失败不得提前生成不完整名次。

## 4. 报告与人工边界

所有模型完成后，在 `<评测目录>/评测结果` 生成：

- `SenseVoice_CER&Whisper-large-v3-turbo_CER_<版本标识>评价报告.md`：双 ASR 的严格/拼音 CER、完整转写、字符错误位置、独立名次、分歧与健康状态。
- `音频交付与文本一致性_<版本标识>自动检查报告.md`：交付原始测量、台词/结构一致性及强制对齐、读法合规、角色路由等未执行项。

音色贴合、角色区分度、自然度、情绪、表演性停顿、伪影主观感受和长时间听觉疲劳由人工试听判断。自动结果的适用边界见 [自动评测与人工盲听边界说明.md](自动评测与人工盲听边界说明.md)。
