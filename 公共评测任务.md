# 公共评测任务

本文定义 `task8.md` 与 `task9.md` 共用的手动评测任务、评分边界和执行前置条件。调用任务只负责提供本次评测目录、人物/参考音频、`text.md`、`ai_deal.json`、待测 `audio_*.wav`、版本标识和输出目录；不得在任务文件中复制或改写本文规则。

## 1. 调用约定

每个调用任务必须显式提供以下配置：

| 配置项 | 含义 |
| --- | --- |
| `<评测目录>` | 本次版本的 `longAudioTestV*` 目录，内含 `text.md`、`ai_deal.json`、角色参考音频与待测 `audio_*.wav`。 |
| `<版本标识>` | 用于报告文件名区分的版本，例如 `V8` 或 `V9`。 |
| `<输出目录>` | `<评测目录>/评测结果`；正式评分必须新建且为空，只有同一次中断才可使用 `--resume`。 |
| `<实际台词串>` | 以本次 `ai_deal.json` 中实际参与合成、且顺序固定的全部台词拼接结果为准；不能复用其他版本的台词数或文本。 |

每次只处理一个模型对应的 `audio_*.wav`。所有原始值、冻结阈值、通过/失败状态、异常位置和可复核证据均输出到 `<输出目录>`。

## 2. 手动执行前置条件

本公共任务不在文档修改时执行任何评测。预检失败时停止，不得用另一版本的长音频、替代模型权重或不同版本的配置继续评分。

- 使用两个隔离的 Conda（Python 环境管理器）环境：`seed_tts_eval` 运行中文 Seed WER（词错误率）及长音频的 SenseVoice / Whisper-large-v3-turbo CER（字符错误率）；`seed_tts_sim` 只运行官方 WavLM-large-SV SIM（说话人余弦相似度）。两者的 PyTorch、Torchaudio、Hydra 依赖不兼容，禁止混装，也不得误用 `audio_eval` 环境。
- 详细的软件版本、Fairseq 安装方式、本地资源、环境变量和非评分导入检查见 [评测环境和配置前提.md](评测环境和配置前提.md)。其中 `seed_tts_eval` 的 `pip check` 必须成功；`seed_tts_sim` 的 S3PRL 训练/数据集可选依赖告警只记录，运行准入以目标入口导入为准。
- 本项目只评测中文。冻结 Seed-TTS-Eval 源码提交、官方测试集 SHA-256、官方 `wavlm_large_finetune.pth` SHA-256、本地 Paraformer、SenseVoiceSmall 与 Whisper-large-v3-turbo 的权重/配置 SHA-256、两个环境的 `pip freeze`、GPU/驱动/CUDA、推理参数、文本规范化配置和随机种子。记录只写入本次结果目录，不写入仓库配置。中文流程不下载、不加载、也不记录原版 `openai/whisper-large-v3`。
- 在运行前制作并冻结 Seed-TTS-Eval 源码兼容补丁及 SHA-256：中文 WER 跳过无评分作用的 `prepare_ckpt.py`；`run_wer.py` 从 `SEED_TTS_PARAFORMER_DIR` 以 `hub="hf"` 加载本地 Paraformer；`cal_wer.sh` 与 `cal_sim.sh` 的 `sudo split` 改为普通用户 `split`；保留将 SIM 目录 `select.py` 改名以避免遮蔽 Python 标准库的补丁；固定 UniSpeech Fairseq 以 `READTHEDOCS=1` 跳过未用的可选原生扩展。补丁不得改变输入、分片规则、ASR 解码、WavLM 嵌入/相似度计算或汇总公式。
- 正式评分前，用一个官方中文样本完成“本地 Paraformer 加载、Whisper-large-v3-turbo 加载、独立 `utt.wav` 生成、WER/SIM 结果文件可写”的烟雾检查。烟雾检查只验证环境，不能作为模型比较结果，也不能覆盖或续跑正式输出目录。

## 3. 可直接参考的自动评测机制

所有数值阈值、交付格式和目标渠道响度规范必须在实际评测前冻结到机器可读配置；没有冻结阈值时，只报告原始测量值和异常位置，不得自行判定通过、失败或优劣。

### 3.1 音频交付硬门槛

- 检查文件可解码性、非空性、哈希、采样率、声道数、位深与时长是否符合冻结的交付契约。
- 按冻结阈值检查削波、最大真峰值、直流偏置、前后静音、非预期长静音、掉音和突然截断。
- 按目标发布渠道的冻结规范检查集成响度、短时响度、响度范围和最大真峰值。

### 3.2 台词与结构完整性

- 对每个模型的完整合成音频，以 `<实际台词串>` 为唯一文本参考，分别计算 SenseVoice CER 与本地 Whisper-large-v3-turbo CER。前者从 `SEED_TTS_SENSEVOICE_DIR` 加载，后者从 `SEED_TTS_TURBO_DIR` 加载；均冻结权重、依赖版本、解码参数和文本规范化配置。
- 核对台词覆盖、缺失、插入、重复和顺序错误，保留完整转写、错误位置和对应原始台词。双 ASR 后端分别报告和排名，不得平均为综合分。
- CER 仅衡量文本保真，不表示音频质量、自然度或角色表现。Whisper-large-v3-turbo CER 不得标作原版 Whisper-large-v3 CER。

### 3.3 文本时间与读法合规

- 使用冻结发音词典的强制对齐，检查字符或词级可对齐覆盖率、未对齐比例、台词/角色覆盖和文本顺序。
- 按冻结阈值检查标点停顿时长、语速和异常静音；中文专有名词、数字、日期和英文读法以冻结词典为准。

### 3.4 角色路由告警（非音色评分）

- 仅在闭集角色分类器、参考音频集、校准方式与阈值均已冻结时，检查每段的目标角色 Top-1 与对其他候选角色的置信差距，输出“疑似串角/错路由”告警。
- 该检查不是 WavLM SIM 或 SpeechBrain ECAPA SIM 的原始分数替代品，不用于音色贴合打分、自动排名或综合分。未配置校准分类器时，报告必须标明“未执行”，不得用原始 SIM 分数补位。

### 3.5 Seed-TTS-Eval 中文零样本音色克隆基准

- 此项是独立于 `<评测目录>` 长音频的标准化外部基准。不得把 `audio_*.wav` 整条成品直接作为 Seed-TTS-Eval 输入，也不得把其分数与长音频结果混合。
- 仅运行官方中文常规集 `zh/meta.lst` 与中文难例集 `zh/hardcase.lst`，不运行英文集。
- 对每个被测模型，严格按每条 meta 的参考音频、参考文本和待合成文本生成独立 `utt.wav`。冻结源码、数据/权重哈希、模型 revision（修订版本）、推理参数、文本规范化配置和随机种子；缺少任一项时，报告不得宣称可复现或可横向比较。
- 使用经兼容补丁处理的官方 `cal_wer.sh`，以本地 Hugging Face（模型托管平台）`funasr/paraformer-zh` 计算两个中文集的官方 WER。中文实现按逐字 token（记号）计分，报告必须标为“Seed-TTS 中文 WER（逐字 token）”；不得与长音频 CER 合并，也不得用 Whisper-large-v3-turbo 替代 Paraformer。
- 使用官方 `cal_sim.sh` 与指定 WavLM-large 说话人验证权重，分别计算两个中文集的 WavLM-large-SV SIM；保留逐条“生成音频—meta 参考音频”配对分数和分集均值。
- WER 与 SIM 仅可在相同官方分集、评测器版本和冻结推理配置下直接比较。分别报告，不得加权成单一分数或与长音频名次合并；它们不表示长音频的自然度、情绪、角色区分度或最终生产排名。

## 4. 公共报告与人工边界

所有模型完成后，在 `<输出目录>` 生成：

- `SenseVoice_CER&Whisper-large-v3-turbo_CER_<版本标识>评价报告.md`：双 ASR 的全文 CER、完整转写、台词错误位置、双后端名次和分歧项。
- `音频交付与文本一致性_<版本标识>自动检查报告.md`：交付硬门槛、台词/结构完整性、强制对齐/读法合规和角色路由告警的逐模型结果。
- `Seed-TTS_ZH_WER&WavLM-large-SV_SIM_<版本标识>标准基准报告.md`：中文常规集与难例集分别列出官方 WER、官方 WavLM-large-SV SIM、逐条证据、冻结版本和推理配置。

除 Seed-TTS-Eval 的官方 WavLM-large-SV SIM 外，不运行直接针对长音频的 WavLM SIM、SpeechBrain ECAPA SIM、UTMOSv2 或 NISQA-TTS，也不生成自动综合分或自动总排名。音色贴合、角色区分度、自然度、情绪、表演性停顿、伪影主观感受和长时间听觉疲劳由用户人工试听后直接判断；不要求盲化播放列表、评分表或独立的 `listener-review` 目录。具体边界见 [自动评测与人工盲听边界说明.md](自动评测与人工盲听边界说明.md)。

> `tts-bench` 保留为长音频 CER、音频健康检查与报告代码的来源；但现有 `run_neutral_evaluation_v8.py`、`run_neutral_evaluation_v9.py` 默认会额外运行已在本公共任务排除的长音频 WavLM、ECAPA、UTMOSv2 与 NISQA。不得直接以默认参数执行这两个旧入口；后续只能复用其中符合本文范围的能力，或另行冻结一个仅含公共指标的受限入口。
